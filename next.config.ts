import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  /**
   * Images: put files in `public/` and reference `/your-file.jpg` in MDX or frontmatter
   * `cover`. For remote `http(s)` URLs, add `images.remotePatterns` (see Next.js docs).
   */
};

export default nextConfig;
