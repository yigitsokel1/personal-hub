generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model SiteSettings {
  id             Int      @id @default(1)
  brandLabel     String   @map("brand_label")
  positioningLine String  @map("positioning_line")
  footerSignature String  @map("footer_signature")
  heroTitle      String   @map("hero_title")
  heroSubtitle   String   @map("hero_subtitle")
  productSignals Json     @map("product_signals")
  footerIntro    String   @map("footer_intro")
  contactEmail   String   @map("contact_email")
  githubUrl      String   @map("github_url")
  linkedinUrl    String   @map("linkedin_url")
  updatedAt      DateTime @updatedAt @map("updated_at")

  @@map("site_settings")
}

model AboutPageContent {
  id        Int      @id @default(1)
  title     String
  intro     String
  sections  Json
  updatedAt DateTime @updatedAt @map("updated_at")

  @@map("about_page_content")
}

model Writing {
  id          String    @id @default(cuid())
  title       String
  slug        String    @unique
  summary     String
  body        String
  tags        String[]
  category    String?
  series      String?
  featured    Boolean   @default(false)
  published   Boolean   @default(false)
  readingTime Int?      @map("reading_time")
  publishedAt DateTime? @map("published_at")
  createdAt   DateTime  @default(now()) @map("created_at")
  updatedAt   DateTime  @updatedAt @map("updated_at")

  @@map("writing")
}

model Project {
  id                     String    @id @default(cuid())
  title                  String
  slug                   String    @unique
  summary                String
  body                   String
  tags                   String[]
  featured               Boolean   @default(false)
  published              Boolean   @default(false)
  publishedAt            DateTime? @map("published_at")
  createdAt              DateTime  @default(now()) @map("created_at")
  updatedAt              DateTime  @updatedAt @map("updated_at")
  role                   String
  stack                  String[]
  platform               String?
  problem                String
  solution               String
  architectureHighlights String[]  @map("architecture_highlights")
  decisions              String[]
  outcomes               String[]
  repoUrl                String?   @map("repo_url")
  liveUrl                String?   @map("live_url")
  timeline               String?

  @@map("projects")
}

model Work {
  id                   String    @id @default(cuid())
  title                String
  slug                 String    @unique
  summary              String
  body                 String
  tags                 String[]
  featured             Boolean   @default(false)
  published            Boolean   @default(false)
  publishedAt          DateTime? @map("published_at")
  createdAt            DateTime  @default(now()) @map("created_at")
  updatedAt            DateTime  @updatedAt @map("updated_at")
  client               String
  engagementType       String    @map("engagement_type")
  role                 String
  timeline             String?
  projectUrl           String?   @map("project_url")
  confidentialityLevel String?   @map("confidentiality_level")
  scope                String[]
  responsibilities     String[]
  constraints          String[]
  impact               String[]

  @@map("work")
}

model Lab {
  id          String    @id @default(cuid())
  title       String
  slug        String    @unique
  summary     String
  body        String
  tags        String[]
  status      String
  liveUrl     String?   @map("live_url")
  featured    Boolean   @default(false)
  published   Boolean   @default(false)
  publishedAt DateTime? @map("published_at")
  createdAt   DateTime  @default(now()) @map("created_at")
  updatedAt   DateTime  @updatedAt @map("updated_at")

  @@map("labs")
}

model ContentSlugRedirect {
  id        String   @id @default(cuid())
  domain    String
  oldSlug   String   @map("old_slug")
  newSlug   String   @map("new_slug")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@unique([domain, oldSlug])
  @@index([domain, newSlug])
  @@map("content_slug_redirects")
}
